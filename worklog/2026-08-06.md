# 2026-08-06 工作日志

## 背景 / 目标

把 NexaBot（[HKUDS/nanobot](https://github.com/HKUDS/nanobot) 的个人 fork）的**本地开发环境完全跑通**，并确定二开的 Git 工作流。

## 完成事项

### 1. Git 工作流（✓ 已完成并验证）

- 添加官方上游远程：`git remote add upstream https://github.com/HKUDS/nanobot.git`
- 决定放弃分支维护，**直接在 `main` 上开发**；删除 `dev` 分支（本地 `git branch -d dev` + 远程 `git push origin --delete dev`）
- 验证「本地 → GitHub」提交推送全流程：`git add . / commit / push origin main`（push test.txt 成功，`a95fd0ee..d6580c93`）
- 确定同步官方的方式：`git fetch upstream` 零副作用预览 → `git pull upstream main` 合并

### 2. Python 环境（uv，✓）

- `uv sync --all-extras --dev` 装齐运行 + 开发依赖（`.venv`）
- `uv run ruff check nanobot/` 通过
- 了解到 `uv.lock` 被 .gitignore 忽略、本地首次生成

### 3. 前端环境（✓）

- 安装 bun：`npm install -g bun`
- `bun install` 装齐前端依赖，`bun run dev` 能起 Vite（5173）

### 4. 运行链路（✓ 摸清）

- 确认本版本端口拓扑：**WebUI 频道 8069 / gateway 18790 / Vite 5173**（`AGENTS.md` 写的 8765 已过时）
- 确定官方启动姿势：`uv run nanobot webui --dev`（一键起 gateway + WebUI 频道 + Vite，自动接线并带鉴权打开浏览器）
- 拿到 WebUI 登录密码：`~/.nanobot/config.json` 里 `channels.websocket.tokenIssueSecret`

### 5. Logo 替换（✓ 已改并验证）

- 用户提供 `openai深色.png`（黑色 logo 透明底）与 `openai浅色.png`（白色 logo 透明底）两张图。
- 替换了 3 处 logo，明暗主题各用一张：
  - **README 封面**：`images/readme-cover-light.png`（黑图）/ `readme-cover-dark.png`（白图），README 的 `<picture>` 按 `prefers-color-scheme` 切换；删除旧 `readme-cover-*.svg`。
  - **WebUI favicon**：`webui/index.html` 用两个带 `media="(prefers-color-scheme: ...)"` 的 `<link rel="icon">` 实现明暗切换。
  - **侧边栏 logo**：`webui/src/components/Sidebar.tsx` 用 `useThemeValue()` 按主题切换两张图。
- 验证：`bun run build` 类型检查 + 打包通过；`app-layout.test.tsx` 43 个测试通过；产物 `nanobot/web/dist/brand/` 含新图。

## 问题与解决

| # | 问题 | 原因 | 解决 |
|---|---|---|---|
| 1 | pytest 报 2244 个 `PermissionError`（拒绝访问 `Temp\pytest-of-LEI`），另有 8 个 failed | pytest 临时目录权限损坏，所有用 tmp_path 的测试全挂 | 杀掉残留 python 进程 + 删除 `pytest-of-LEI` 目录；**全量重跑待验证** |
| 2 | `bun` 命令找不到 | bun 未安装 | `npm install -g bun` |
| 3 | Vite 代理报 `ECONNREFUSED 127.0.0.1:8765` | 8765 是旧默认端口，本版本 gateway 在 18790、WebUI 频道在 8069，手动起的前端没指向正确端口 | 改用 `nanobot webui --dev` 自动设置 `NANOBOT_API_URL=http://127.0.0.1:8069` |
| 4 | WebUI 打开后弹登录框 | `websocketRequiresToken=true` 且 config 里已生成 `tokenIssueSecret` | 密码 = 该 secret 值（或直接用 `nanobot webui --dev` 自动带 secret，免输入） |

## 待办 / 下一步

- [ ] 全量重跑 `uv run pytest`，确认 2244 个 ERROR 消失；复查剩余 **8 个 failed**（集中在 whatsapp 频道）
- [ ] 确认 WebUI 能正常完成一次对话（运行时模型已切到 deepseek-v4-flash）
- [ ] 微信频道 `session expired (errcode -14)`：不用的可在 config 里关闭 `channels.weixin`
- [ ] 执行 `cd webui && bun run build`，消除「Bundled WebUI build is missing」警告（打包 wheel 前必须做）
- [ ] 把 `STARTUP.md` 和 `worklog/` 提交并 push 到 GitHub
